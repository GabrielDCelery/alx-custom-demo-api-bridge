'use strict';

module.exports = {
    host: require('./host'),
    alxAPI: require('./alxAPI'),
    auth: require('./auth'),
    targetGroups: require('./targetGroups'),
    auth0: require('./auth0'),
    files: require('./files')
};
